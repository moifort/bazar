{
  "name": "bazar",
  "private": true,
  "scripts": {
    "prepare": "nitro prepare",
    "build": "nitro build",
    "dev": "nitro dev --host 0.0.0.0",
    "lint": "bunx biome check",
    "lint:fix": "bunx biome check --write",
    "test": "bun test",
    "test:unit": "bun test .unit.test",
    "test:int": "bun test .int.test",
    "test:feat": "bun test .feat.test",
    "test:coverage": "bun test --coverage",
    "generate:graphql": "bun scripts/export-graphql-schema.ts",
    "generate:ios": "./scripts/apollo-codegen.sh",
    "generate": "bun run generate:graphql",
    "bootstrap": "./scripts/bootstrap.sh",
    "destroy": "./scripts/teardown.sh",
    "infra:build": "bun install --frozen-lockfile && bunx nitro prepare && bun run generate:graphql && bun run build",
    "infra:init": "cd infra && ../scripts/tf init",
    "infra:plan": "bun run infra:build && cd infra && ../scripts/tf plan",
    "infra:apply": "bun run infra:build && cd infra && ../scripts/tf apply"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "@types/bun": "1.3.14",
    "@types/lodash-es": "4.17.12",
    "@types/node": "26.1.1",
    "bun-types": "1.3.14",
    "h3": "1.15.11",
    "nitropack": "2.13.4",
    "ts-brand": "0.2.0",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "@apollo/server": "5.5.1",
    "@pothos/core": "4.13.1",
    "consola": "3.4.2",
    "dataloader": "2.2.3",
    "firebase-admin": "13.10.0",
    "graphql": "16.14.2",
    "lodash-es": "4.18.1",
    "ts-pattern": "5.9.0",
    "zod": "4.4.3"
  }
}
