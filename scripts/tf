#!/usr/bin/env bash
#
# tf — thin wrapper that ensures the pinned terraform binary exists,
# then execs it with the passed arguments.

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
"$ROOT/scripts/install-terraform.sh"
exec "$ROOT/infra/.bin/terraform" "$@"
