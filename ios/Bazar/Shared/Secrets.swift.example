// Template — copy to Secrets.swift (gitignored).
// Reserved for future client-side secrets. Authentication uses Firebase ID
// tokens (Sign in with Apple) — no API token to configure.
enum Secrets {}
